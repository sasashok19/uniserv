# ============================================================
# UniServe :: ai-core :: Python 3.11 dependencies (Phase 1)
# ============================================================

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.7.0

# OpenAI Assistants API (Feature 06 conversation agent)
openai==2.44.0

# PII scrubbing (wired in 07_PII_SCRUBBER; needs a spaCy model at that point)
presidio-analyzer==2.2.355
presidio-anonymizer==2.2.355

# Event bus (Valkey, Redis-compatible)
valkey==6.0.2

# Identity resolver — phone normalisation (Feature 03)
phonenumbers==8.13.52

# Outbound HTTP to db-writer / api-gateway
httpx==0.28.1

# --- dev / test ---
pytest==8.3.4
